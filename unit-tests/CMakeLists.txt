cmake_minimum_required(VERSION 3.0)
if(BUILD_UNIT_TESTING)
  set(PFUNIT_SRC ${CMAKE_CURRENT_SOURCE_DIR}/pFUnit CACHE INTERNAL "PFUNIT SOURCE DIR")
  add_subdirectory(pFUnit)
# add_subdirectory(pFUnit/source)
# add_subdirectory(pFUnit/bin)
  add_subdirectory(tests) 
endif()
