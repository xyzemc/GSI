These are the scripts for running the community GSI regression tests.

1. Run the ensemble of tests using the script "gsi_test_wrapper." See the header of that script for details.

2. Check the output against a baseline test with the script "compare_script.ksh."

3. Check for self-consistency of the run with respect to differing processor counts using the script "selfcompare_script.ksh."

Data directory structure.




Run
